# Local development only. init_dev.py generates missing passwords and the token.
# Never commit .env or print its contents. No cloud credentials are required.
POSTGRES_PASSWORD=
HA_USERNAME=hirz
HA_PASSWORD=
HA_TOKEN=
# Quoted multiline PKCS#8 PEM, generated only after confirming no audit history.
# Restore a lost key from backup; never replace one used by existing audit rows.
AUDIT_SIGNING_KEY=
